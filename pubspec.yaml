name: light_console_sdk
description: Console coexistence SDK for ShowUp — co-pilot and stunt double for professional lighting consoles.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  collection: ^1.19.1

dev_dependencies:
  test: ^1.25.0
  lints: ^4.0.0
